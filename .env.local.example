# Copy to .env.local and fill in. .env.local is gitignored - never commit it.
# Key: Obsidian -> Settings -> Community plugins -> Local REST API -> API Key
OBSIDIAN_API_KEY=
# Optional. Defaults to 27125 (the HTTP endpoint connect-jarvis.ps1 uses).
OBSIDIAN_PORT=27125
